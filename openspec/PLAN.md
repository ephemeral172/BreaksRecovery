# BreaksRecovery — План разработки

> **Легенда:** ✓ — сделано · ✓ *пояснение* · ~ — частично · — — не сделано

**Статус (18.06.2026):** проект **~42%** · Робот 1 **~50%** · Фаза 0 **~95%** · GET DATA **100%** (test)

---

## Фаза 0 — Каркас и инициализация (~95%, закрыта 18.06)

### Документация и спецификация

| # | Задача | Результат |
|---|---|---|
| D-01 | Создать `openspec/project.md` (стек, архитектура, БД, правила) | ✓ |
| D-02 | Создать `openspec/progress.md` (трекинг задач) | ✓ |
| D-03 | ADR-001: Архитектура запуска WF-2 (Execute Workflow, не Cron) | ✓ |
| D-04 | ADR-002: Стратегия загрузки в WFMS (WF-1 пакет, WF-2 по 1 сотруднику) | ✓ |
| D-05 | ADR-003: Схема таблиц БД RPA | ✓ |
| D-06 | ADR-004: Конфиг в Git/JSON, не в БД | ✓ |
| D-07 | ADR-005: Обработка ошибок — Reframework Error Handler | ✓ |
| D-08 | ADR-006: Схема `n8n_breaks_recovery` + `agents` | ✓ |
| D-09 | ADR-007: WFM SQL — технические ловушки | ✓ |
| D-08 | Изучить и зафиксировать TDR (SQL-схема, взаимодействие с системами) | ✓ |
| D-09 | Создать `openspec/PLAN.md` с поэтапным планом | ✓ |
| D-10 | Зафиксировать модель данных: cfg_* vs WFM vs транзакции (`project.md`) | ✓ |

### Доступы и credentials

| # | Задача | Результат |
|---|---|---|
| A-01 | Получить доступ к БД RPA Data (`bpa-primo-prod-db01.msk.avito.ru`) | — |
| A-02 | Получить доступ к тестовой БД WFM (`wfm-test-avito.dc.oswfm.ru`) | ✓ |
| A-03 | Получить доступ к Stash (`projects/RPA/repos/n8n`) | ~ |
| A-04 | Настроить credential `WFM DB` в n8n (PostgreSQL, read-only) | ✓ |
| A-05 | Настроить credential `RPA DB` в n8n (PostgreSQL, insert/update/select) | ✓ прогнан |
| A-06 | Получить и настроить Mattermost Bot Token | ~ Continue On Error |
| A-07 | Настроить credential `SMTP_Fallback` (`exchange.avito.ru:587`) | — |

### Таблицы БД RPA (схема `n8n_breaks_recovery`, ADR-006)

| # | Задача | Результат |
|---|---|---|
| DB-00 | Создать таблицу `agents` (ПДн) | ✓ |
| DB-01 | Создать схему + `breaks_recovery_transactions` (`agent_id` FK) | ✓ прогнан |
| DB-02 | Создать таблицу `breaks_balance_transactions` | ✓ |
| DB-03 | Создать таблицу `breaks_balance_moves` | ✓ |
| DB-04 | Создать таблицу `cfg_activity_mapping` | ✓ |
| DB-05 | Создать таблицу `cfg_non_standard_containers` | ✓ |
| DB-06 | Создать таблицу `cfg_news_reading` | ✓ |
| DB-07 | Создать таблицу `cfg_news_exception_skills` | ✓ |
| DB-08 | Создать таблицу `cfg_fte_groups` | ✓ |
| DB-09 | Создать таблицу `cfg_email_fallback` | ✓ |
| DB-10 | Заполнить `cfg_activity_mapping` (153 строки, ТЗ §4.5 шаг 11) | ✓ |
| DB-11 | Заполнить `cfg_non_standard_containers` (53 строки, ТЗ §4.5 шаг 8) | ✓ |
| DB-12 | Заполнить `cfg_news_reading` (77 строк, ТЗ §4.5 шаг 9) | ✓ |
| DB-13 | Заполнить `cfg_news_exception_skills` (2 строки, ТЗ §4.5 шаг 9) | ✓ |
| DB-14 | Заполнить `cfg_fte_groups` (161 строка, ТЗ §4.5 шаг 24) | ✓ |
| DB-15 | Подготовить SQL `workflows/sql/init_cfg_data.sql` (INSERT для cfg_*) | ✓ |
| DB-17 | Применить `init_cfg_data.sql` на non-prod RPA DB | ✓ 153/53/77/2/161/0 |
| DB-16 | Заполнить `cfg_email_fallback` (email команды RPA, TDR §7.5) | — |

### Воркфлоу: каркас

| # | Задача | Результат |
|---|---|---|
| WF-01 | Создать и проверить `GetConfig` (JSON + Validate) | ✓ прогнан |
| WF-02 | Создать воркфлоу `InitTables` (CREATE SCHEMA + 10 таблиц) | ✓ прогнан |
| WF-03 | Создать воркфлоу `ErrorHandler` (нормализация + GetConfig + UPDATE + MM) | ✓ JSON; прогон — |
| WF-04 | `BreaksRecovery_Main`: Phase 1 INIT | ✓ E2E non-prod |
| WF-04b | `BreaksRecovery_Main`: Phase 2 GET DATA | ✓ E2E test 18.06 (4918 агентов) |
| WF-05 | Развести credentials в `Main`: транзакции → RPA DB, чтение → WFM DB | ~ RPA ✓, WFM ✓ |
| WF-06 | Добавить Set Edit Fields (`error_type`/`error_message`) на всех ветках ошибок | — |
| WF-07 | Переделать `BreaksRecovery_Balance`: добавить credentials + Error Handler | ~ |
| WF-08 | Настроить Cron: `0 2 * * *` UTC = 05:00 МСК | ✓ |
| WF-09 | Создать папку проекта в Stash, создать ветку `dev/breaks-recovery` | — |

> **Фаза 0:** закрыта (18.06). **Фаза 1 GET DATA:** закрыта на test. **Следующий шаг:** WF1-02…04 (SQL шаги 4–6), бизнес-логика шагов 8–11.

---

## Фаза 1 — WF-1: Восстановление (~50%)

> **GET DATA закрыт** (18.06, test: 4918 агентов). Далее: SQL шаги 4–6, логика 8–11, WFMS.

### SQL-запросы к БД WFM

| # | Задача | Результат |
|---|---|---|
| S1-01 | SQL: агенты на смене D+1 + фильтр cfg_fte_groups (WF1-00/01) | ✓ test + Main |
| S1-02 | SQL: расписание агента + контейнер + схема + плавающие/фикс перерывы | — |
| S1-03 | SQL: навыки агента (`user_skill_mapping` + `skill`) | — |
| S1-04 | SQL: история активностей за 7 дней (для расчёта пропущенных дней новостей) | — |
| S1-05 | Проверить SELECT-ом на тестовой БД: timestamp = UTC, `skill.time_zone = Europe/Moscow` | ✓ ADR-007 |

### Бизнес-логика (Code-ноды)

| # | Задача | Результат |
|---|---|---|
| L1-01 | Логика: определение типа контейнера (стандартный 9/12hrs / нестандартный / не распознан → BE2) | ~ заглушка |
| L1-02 | Логика: расчёт положенных перерывов/обеда по контейнеру | ~ заглушка |
| L1-03 | Логика: расчёт минут чтения новостей (таблица + надбавки за пропуски, макс 60 мин) | ~ заглушка |
| L1-04 | Логика: определение недостающих/перекрытых активностей (сверка с расписанием) | ~ заглушка |
| L1-05 | Логика: расчёт слотов размещения (1-2 ч, «да»-активности, кратно 5 мин; нет слота → BE3) | ~ заглушка |
| L1-06 | Логика: формирование строк пакета загрузки (login, activity, start, end, timeZone) | ~ заглушка |

### Загрузка в WFMS

| # | Задача | Результат |
|---|---|---|
| U1-01 | Авторизация в WFMS web UI (LDAP, сессионный cookie) | ~ заглушка |
| U1-02 | Загрузка файла-пакета всех агентов в WFMS | ~ заглушка |
| U1-03 | «Опубликовать изменения» → «Пересчитать FTE» | ~ заглушка |

### Транзакции и уведомления

| # | Задача | Результат |
|---|---|---|
| T1-01 | Запись транзакций в `breaks_recovery_transactions` (agents + agent_id, ADR-006) | ✓ |
| T1-02 | Уведомление в Mattermost: Success / Warn / BE (дневная сводка) | ~ Continue On Error |
| T1-03 | Триггер WF-2 через Execute Workflow после завершения WF-1 | ✓ |

---

## Фаза 2 — WF-2: Балансировка покрытия (~5%)

> Стартует после WF-1 в prod. Черновик воркфлоу есть; бизнес-логика не начата. Блокер: Q-03 (порог просадки).

| # | Задача | Результат |
|---|---|---|
| S2-01 | SQL: план/факт FTE по 15-мин интервалам для группы линий/навыков | — |
| L2-01 | Логика: выявление диапазонов просадки (объединение смежных интервалов) | — |
| S2-02 | SQL: агенты-кандидаты на перенос (перерывы/обеды в диапазоне просадки) | — |
| L2-02 | Алгоритм балансировки: сортировка кандидатов → поиск слота в профиците → условие «не навреди» | — |
| L2-03 | Обновление внутренней модели покрытия после каждого принятого переноса | — |
| U2-01 | Загрузка пакета переносов в WFMS (по 1 сотруднику + публикация + пересчёт FTE) | — |
| T2-01 | Запись транзакций в `breaks_balance_transactions` + `breaks_balance_moves` | — |
| T2-02 | Уведомление: дневная сводка WF-2 (переносов, просадок без кандидатов) | — |

---

## Фаза 3 — Тестирование и ОПЭ

| # | Задача | Результат |
|---|---|---|
| TST-01 | Подготовить тестовые данные (стандарт 9/12hrs, нестандарт, ночная смена, просадка) | — |
| TST-02 | Прогон WF-1 на тестовой БД WFM — проверить пакет и транзакции | — |
| TST-03 | Прогон WF-2 на тестовых данных — проверить переносы и сводку | — |
| TST-04 | Code Review (PR в Mattermost-канал) | — |
| TST-05 | Согласование выхода в ОПЭ с Process Owner (aakrasyukov) | — |
| TST-06 | Переключить Cron на 13:00 МСК для ОПЭ | — |
| TST-07 | Мониторинг первые 3 дня ОПЭ, сверка с WFM | — |
| TST-08 | Переключить Cron на 19:30 МСК после ОПЭ | — |

---

## Открытые блокеры

| # | Что ждём | От кого |
|---|---|---|
| Q-01 | **Точные названия активностей** («Перерыв», «Обед», «Обучение (Новости)») | WFM (aakazarina) |
| Q-03 | Числовой порог просадки FTE | aakrasyukov |
| Q-04 | Полный список навыков-исключений (+15 мин) | aakazarina |
| Q-06 | Разбивка нестандартных контейнеров (кол-во × длительность) | rsramaev |
